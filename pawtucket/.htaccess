#
# Apache/PHP/Drupal settings:
#

AuthType Basic
AuthName "Halt! Who goes there? Wrong turn!"
AuthUserFile "/opt/htpasswds/weblockout"

# Allow any valid user
<RequireAny>
    <RequireAll>
            # this example is using the alternate matching form here so we don't have
            # to escape the /'s in the URL.
            Require expr %{REQUEST_URI} =~ m#^/.well-known/.*#
   </RequireAll>
   Require valid-user
 </RequireAny>

#Require valid-user

# Show directory listings for URLs which map to a directory.
Options +Indexes

# Display details
IndexOptions +FancyIndexing